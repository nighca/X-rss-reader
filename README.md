X-rss-reader
============

RSS reader based on X

### install dependencies

	bower install

	npm install

	npm install -g react-tools

### watch

	jsx --watch src/ build/

### build

	npm run browserify